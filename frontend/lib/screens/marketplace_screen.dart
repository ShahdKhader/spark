import 'package:flutter/material.dart';
import '../widgets/navigation_menu.dart';

class MarketplaceScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Market Page Content')),
    );
  }
}
